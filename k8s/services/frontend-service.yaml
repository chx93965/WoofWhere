apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  labels:
    app: frontend
spec:
  type: LoadBalancer
  selector:
    app: frontend
  ports:
    - port: 8001
      targetPort: 80
      protocol: TCP
      name: http
  sessionAffinity: None